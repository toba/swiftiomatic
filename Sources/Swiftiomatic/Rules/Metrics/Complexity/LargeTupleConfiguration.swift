struct LargeTupleConfiguration: RuleConfiguration {
    let id = "large_tuple"
    let name = "Large Tuple"
    let summary = "Tuples shouldn't have too many members. Create a custom type instead."
}
