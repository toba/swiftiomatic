struct FileNameConfiguration: RuleConfiguration {
    let id = "file_name"
    let name = "File Name"
    let summary = "File name should match a type or extension declared in the file (if any)"
    let isOptIn = true
}
