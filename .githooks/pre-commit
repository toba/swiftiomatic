#!/bin/bash
# Pre-commit hook: run fast tests to catch regressions before push.
# Install: git config core.hooksPath .githooks
set -euo pipefail

echo "Running fast tests..."
SWIFTIOMATIC_FAST_TESTS=1 swift test --disable-sandbox 2>&1 | tail -5

echo "Pre-commit checks passed."
